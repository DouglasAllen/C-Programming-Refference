This is sample MinGW project based on Qt framework from 'Top features
you need to know about' chapter of the 'MinGW Starter' book (ISBN 5626).

The project contains next file:

main.cpp - C++ source file with main function

This file is described detailed in the book.

This is commands to build the project:

$ qmake -project
$ qmake
$ mingw32-make

Last command depends to GNU Make utility executable name in your installation.
It may be changed to:

$ make
