This is sample MinGW project with STL vector of char type elements
sorting implementation from 'Top features you need to know about' chapter
of the 'MinGW Starter' book (ISBN 5626).

The project contains next files:

sorting.cpp - C++ source file with main function
Makefile - GNU Make utility rules file to build project

All these files are described detailed in the book.

This is command to build the project:

$ make
